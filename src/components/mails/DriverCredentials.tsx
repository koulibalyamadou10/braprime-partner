export default function DriverCredentials(email: string, password: string) {
    return (
        <div>
            <h1>Driver Credentials</h1>
            <p>Email: {email}</p>
            <p>Password: {password}</p>
        </div>
    )
}